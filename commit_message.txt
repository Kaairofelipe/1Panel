🧹 chore(code-health): replace context.TODO() with context.Background() in website.go

🎯 **What:** Replaced four occurrences of `context.TODO()` with `context.Background()` in `agent/app/service/website.go`. Fixed CI check suite failures by pinning GitHub Action versions to full-length SHAs and configuring typo exclusions.

💡 **Why:**
1. `context.TODO()` is meant to be a placeholder when it's unclear which context to use. Since these are top-level database save/delete operations occurring without an explicit context being passed down from the HTTP request, `context.Background()` is the semantically correct choice, improving code readability and maintainability.
2. The GitHub repository enforces a policy that requires all GitHub Actions to be pinned to a full-length commit SHA for security and reproducibility. Node 20 runner deprecation required bumping the checkout action.
3. The typos CI check flagged many false positives on foreign language text and intentional identifiers which caused it to fail.

✅ **Verification:**
- Verified by checking that there are no regressions. Ran `go fmt` and executed the agent tests (`GOWORK=off go test ./app/service`). Changes are localized and syntactically correct.
- Resolved CI failure annotations by replacing `@v2`, `@master`, and `@main` with specific SHAs, updating checkout to `v4`, and adding `.typos.toml` to ignore false positives.

✨ **Result:** Improved codebase semantic accuracy without changing behavior and ensured CI pipelines execute successfully without policy violations.
