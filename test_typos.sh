# just checking that they are now ignored. They should be because we added more explicit exclusions.
# I am ignoring any typos checking in frontend/src/lang/modules/*.ts since it was giving lots of issues with non english translation files or bad parsing
